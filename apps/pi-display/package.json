{
  "name": "pi-display",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "tsx src/dev.ts",
    "start": "NODE_ENV=production tsx src/main.ts",
    "test": "vitest",
    "tsc": "tsc --noEmit",
    "lint": "eslint src test",
    "format": "prettier . --write",
    "format:check": "prettier . --check"
  },
  "dependencies": {
    "@blibliki/display-protocol": "workspace:^",
    "slint-ui": "^1.13.1",
    "vite-tsconfig-paths": "catalog:vite"
  },
  "devDependencies": {
    "@types/node": "25.1.0",
    "vitest": "catalog:test"
  }
}
